# Press Flow Frontend

React + Vite dashboard for batch monitoring and publish operations.

## Run

```bash
cd frontend
npm install
npm run dev
```

The frontend expects backend API at `http://localhost:8000`.

